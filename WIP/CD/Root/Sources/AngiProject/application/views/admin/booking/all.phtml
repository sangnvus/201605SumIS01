<div class="panel panel-default">
  <!-- Default panel contents -->
  <div class="panel-heading">
    <h2>Danh sách đặt chỗ</h2>
  </div>
  <div class="panel-body">
    
  <!-- Table -->
<table class="table table-bordered">
    <thead>
        <tr>
            <th class="col-lg-1">ID</th>    
            <th>Ngày đặt</th>    
            <th>Ngày tạo</th>    
            <th>Cửa hàng</th> 
            <th>Người đặt</th> 
            <th class="text-center col-lg-2">Trạng thái</th>
            <th class="col-lg-2"></th>
        </tr>
    </thead>
    <tbody>
        <?php if ($rows > 0){ ?>
        <?php foreach ($result as $item) {?>
        <tr>
            <td><?php echo $item->bookingID; ?></td>
            <td><?php echo $item->dateBooking; ?></td>  
            <td><?php echo $item->dateCreateBo; ?></td>   
            <td><?php echo $item->restaurantName; ?></td>
            <td><?php echo $item->userName; ?></td>
            <td class="text-center"><?php 
                switch ($item->statusBo){
                    case 0:
                        echo 'Mới đặt';
                        break;
                    case 1:
                        echo 'Đã xử lý';
                        break;
                    case 10:
                        echo 'Hoàn thành';
                        break;
                }
            ?></td>
            <td class="text-center">
            <a class="btn btn-info" href="<?php echo base_url()?>admin/show_booking/<?php echo $item->bookingID; ?>"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Xem</a>
            </td>
        </tr>
        <?php } ?>
        <?php } ?>
    </tbody>       
  </table>        
  <div class="row">
      <div class="col-lg-12">
        <?php echo $pagination; ?>
      </div>  
  </div>
  </div>
      
</div>
<script type="text/javascript">
    window.bookingModule = window.bookingModule || {};
    
    bookingModule.delete = bookingModule.delete || function(name){
        if (confirm('Xóa ' + name + '?'))
            return true;
        return false;
    }
</script>